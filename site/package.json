{
  "name": "@tscircuit/schematic-trace-solver",
  "version": "0.0.47",
  "type": "module",
  "scripts": {
    "start": "cosmos",
    "build": "tsup-node lib/index.ts --format esm --dts",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "gif": "node scripts/generate-cleanup-gif.mjs"
  },
  "devDependencies": {
    "biome": "^1.5.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "graphics-debug": "^0.0.62"
  },
  "dependencies": {
    "@tscircuit/math-utils": "^0.0.22",
    "bun-match-svg": "^0.0.13"
  }
}
